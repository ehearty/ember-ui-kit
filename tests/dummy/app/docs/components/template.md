`ember-ui-kit` components are divided into 4 namespaces: `ui`, `in`, `fm` and `x`.

- `ui` are generic components.
- `in` are input components.
- `fm` are form components.
- `x` are helper components.
